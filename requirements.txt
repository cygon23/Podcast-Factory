# Core (always required)
PyYAML>=6.0
python-dotenv>=1.0

# Cloud TTS provider SDKs - install only the ones for the engine(s) you'll
# actually use (see OPERATOR_TODO.md). Importing dorosak_factory.tts.engines
# does not fail if one of these is missing; only using that specific engine
# does.
openai>=1.0
azure-cognitiveservices-speech>=1.40
google-cloud-texttospeech>=2.20
boto3>=1.34
elevenlabs>=1.0

# Local TTS (optional, heavy - pulls in torch). Only needed for --engine
# kokoro; the operator downloads the model separately (see OPERATOR_TODO.md).
# kokoro>=0.9

# Local Arabic TTS for the course audio pipeline (--arabic-engine piper).
# Lightweight (onnxruntime, no torch). Voice model downloaded separately
# (see docs/superpowers/specs/2026-07-19-course-audio-pipeline-design.md).
piper-tts>=1.5

# Testing
pytest>=8.0
