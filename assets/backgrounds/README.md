`default.png` here is a **placeholder** (plain dark solid color, 1920×1080) —
generated to prove the video pipeline works end to end, not real branding.

Replace it with actual Dorosak branded artwork before publishing anything.
See `docs/ASSETS.md` for the exact requirements (resolution, per-category
naming convention `cat{N}.png`).
